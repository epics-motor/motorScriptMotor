# Use the following lines if motorScriptMotor was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorScriptMotor was built outside motor
#!MOTOR_SCRIPTMOTOR=
#!-include $(MOTOR_SCRIPTMOTOR)/configure/RELEASE.local
